# Dependencias de la app web INSEVIG (migración a Reflex).
# Fuente de verdad: pyproject.toml. Instalar en dev con:  pip install -e ".[web,dev]"
# En el Windows Server:  pip install -r requirements.txt   (idealmente con hashes/pin exacto)

# --- núcleo (core/) ---
pydantic>=2.8
pydantic-settings>=2.4
pandas>=2.2
pyodbc>=5.1
supabase==2.7.4
pyyaml>=6.0
reportlab>=4.1
openpyxl>=3.1
xlsxwriter>=3.2
jinja2>=3.1
httpx>=0.27

# --- web (insevig_web/) ---
reflex==0.9.10.post1
sqlmodel>=0.0.21
alembic>=1.13
bcrypt>=4.1
itsdangerous>=2.1
msal>=1.28
